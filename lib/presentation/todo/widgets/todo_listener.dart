import 'package:flutter/material.dart';

import 'todo_body.dart';

class TodoListener extends StatelessWidget {
  const TodoListener({super.key});

  @override
  Widget build(BuildContext context) {
    return const TodoBody();
  }
}
